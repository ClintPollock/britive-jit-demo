[project]
name = "britive-jit-demo-mcp"
version = "0.1.0"
description = "MCP server: Claude queries a demo database via Britive ephemeral, just-in-time credentials."
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "pymysql>=1.1.0",
]

[tool.uv]
package = false
